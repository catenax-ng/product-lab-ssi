apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vsissuer.fullname" . }}-test-connection"
  labels:
    {{- include "vsissuer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vsissuer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
